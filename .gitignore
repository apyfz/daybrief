# macOS
.DS_Store

# Swift Package Manager (Package.resolved IS committed — this is an app, pin deps)
.build/
.swiftpm/

# Xcode / generated project (project.yml is the source of truth — regenerate with `xcodegen generate`)
Daybrief.xcodeproj/
DerivedData/
xcuserdata/
*.xcuserstate
