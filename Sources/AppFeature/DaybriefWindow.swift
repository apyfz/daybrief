import Foundation

/// Shared identifiers for the app's window scenes, referenced by both the `Window`
/// scene in the app target and the menu-bar panel that opens it.
public enum DaybriefWindow {
    /// The standalone setup / onboarding / settings window.
    public static let mainID = "daybrief-main"
}
